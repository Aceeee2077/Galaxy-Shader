# Validation — 2026-09-13

**GPU 工程检查通过；完整 Minecraft/Iris 游戏内验收尚未完成。**

1.5.0：新增程序化降雨 pass、水面扩散雨纹与地表冲击、平滑天气联动、晨昏树冠光柱及高度/山谷/远景地形雾后，完整矩阵 2,048 组入口配置 / 去重 1,062 对程序全部编译链接通过；Clear、Cloudy、Sunset、Night、Rain、Thunder、Underwater、Cave、Nether、End 十类合成场景与六档预设回归通过。实际 Iris 1.11.2 解析器继续确认 8 个目标格式、5 个清屏颜色及持久化 `colortex7`。这些是工程验证，不等同于 Minecraft 游戏内画面与 FPS 验收。

1.4.0：新增 TAA 持久化历史、PCSS-like 阴影、GTAO-style AO、SSR 稳定性、三种色调映射与体积光抖动后，完整矩阵 2,048 组入口配置 / 去重 1,014 对程序全部编译链接通过；九类合成场景、六档预设、PBR/相机组合与时域持久化回归通过。实际 Iris 1.11.2 解析器确认 8 个目标格式、5 个清屏颜色及 `colortex7Clear=false`。仍待 Minecraft 游戏内验收与 FPS 实测。

1.3.0：末地行星改为独立公转轨道/速度/视尺寸，并加入每颗行星的受光侧大气辉光后重跑完整矩阵，2,048 组入口配置 / 去重 1,014 对程序全部编译链接通过；九类合成场景离屏回归通过；末地天空六项夹具检查通过。仍待实机游戏验收。

1.2.0：性能优化（云阴影逐顶点化、水体片元早退、远距离 PCF 降采样）后重跑完整矩阵，2,048 组入口配置 / 去重 1,014 对程序全部编译链接通过，九类合成场景离屏回归通过。仍待实机游戏验收。

1.1.0：新增末地行星天空后重跑完整矩阵，并补充末地天空合成夹具：前景遮挡不变、轨道随时间运动、速度为 0 静止、0.5/1/2 倍速度时钟归零一致、关闭后静态、不泄漏至主世界/下界，全部通过。实际 Iris 1.10.9/1.11.2 预处理器与渲染目标解析通过。

1.0.2：第二份用户日志揭示未定义的缓冲区格式标识符。1.0.0/1.0.1 的 GPU 验证器错误地注入了格式宏，因此其编译通过不能作为这些标识符正确性的证据。现已移除此注入，直接用展开的交付源码复现旧版本错误并确认修复版编译通过。实际 Iris 1.10.9/1.11.2 预处理及配置解析均保留了 7 个正确格式和 4 项正确清屏值。以下矩阵已在修正后的验证器上重跑；游戏内重新加载仍待确认。

1.0.1：用户实际游戏日志确认 1.0.0 在 Iris 配置解析阶段失败，具体是单参数 `vec4` 清屏颜色。现已改为四个显式分量；用实际 Iris 1.10.9 与 1.11.2 的 `ConstDirectiveParser` / `DispatchingDirectiveHolder` 验证四项配置及数值通过。已增加 3 项回归测试，并重新运行以下 GPU 检查。修复后的游戏内重新加载仍待验证。

| 检查 | 结果 |
| --- | --- |
| Shader 入口 | 256 个 `.vsh/.fsh` 文件，32 组程序 × 根目录/三个维度路径 |
| 配置 | 35 个选项（新增天气质量、地形雾质量、雨线/雨纹/光柱强度），六档预设，中英文菜单，无不可达选项；默认值对应 HIGH |
| Include / 文件 / draw buffer | 静态检查通过 |
| GPU 编译 | 2,048 个入口配置组合，去重后实际编译并链接 1,062 对程序，全部通过 |
| 编译覆盖 | 六档预设、高级效果关闭、强制 PBR/相机效果；有/无 LabPBR 格式宏；所有维度路径 |
| 渲染 | 10 类合成场景（含 Cloudy / Rain / Thunder）+ 6 档画质 + 1 组 PBR/相机效果，全部通过 |
| 末地天空夹具 | 前景遮挡不变、轨道随时间运动、速度为 0 静止、0.5/1/2 倍速度时钟归零一致、关闭后静态、不泄漏至其他维度，全部通过 |
| 数值与 framebuffer | 8 个颜色目标和每个后处理阶段无 NaN/Infinity；最终结果非空白；无 OpenGL 错误 |
| 可重复性 | 相同状态重复渲染得到逐像素相同的 8-bit 结果 |
| TAA 持久化 | 静止相同输入跨帧累计的最大 8-bit 像素差为 0 |
| 静止相机动态模糊 | 开/关的最大 8-bit 像素差为 0 |
| 方块 ID | 对照本机 Minecraft 26.2 客户端，123 个映射 ID 均存在 |
| Iris 接口 | 核对官方 1.11.2+mc26.2 发布包的依赖和恢复传统深度约定的兼容层 |

GPU：NVIDIA GeForce RTX 5080 Laptop GPU，驱动 616.64，OpenGL 4.6，GLSL 4.60。离屏场景为 640×360 的工程夹具，**不能用于推算 1080p Minecraft FPS**。

GPU 编译读取本包完整 GLSL Include 树，但没有运行 Iris 的实际运行期转换器。离屏渲染使用自建方块模型和纹理，不使用 Minecraft 的区块、实体、GUI 或资源包管线。因此不能据此宣称 A–N 游戏验收已完成。

仓库 `validation/` 内提供完整 JSON 报告、合成场景 PNG、Iris 深度兼容性核查记录和未执行的游戏验收矩阵。打包工具仅在编译/渲染报告的 Shader SHA-256 与当前源码一致时生成 ZIP，并验证压缩包 CRC、根目录及逐文件内容。
